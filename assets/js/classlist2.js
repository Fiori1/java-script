let btn = document.getElementById('botao');

btn.addEventListener('click', function trocar() {
    let div = document.getElementById('titulo');
    if(div.classList.constains("verde"){
        div.classList.remove("verde")
    })
})